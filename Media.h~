#include <iostream>
#include <cstring>

#ifndef MEDIA_H
#define MEDIA_H

class Media{
 public:
  Media(char*, int);
  char* getTitle();
  int getYear();
  ~Media();
  virtual int getType();
 private:
  char* title;
  int year;
};

#endif
